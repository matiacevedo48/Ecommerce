<%= form_with(model: base_product, local: true) do |form| %>
  <% if base_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(base_product.errors.count, "error") %> prohibited this base_product from being saved:</h2>

      <ul>
      <% base_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
